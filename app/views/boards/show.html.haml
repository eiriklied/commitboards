- title @board.name
= content_for :menu do
  .menu
    %h1= @board.name
    %ul.menu-items
      %li= link_to 'Install', '#', class: 'js-install-link'
      /%li= link_to 'Admin', board_admin_path(@board)

.with-menu.center

  %div{id: 'installation-instructions', class: @board.commits.present? ? 'hidden' : ''}
    .spacing-top.spacing-bottom
      %p Now install your board to start uploading selfies.
      %p Just paste the command below into your shell to install to your project.
      %input{id: 'code-example', class: 'code-example', type: 'text', data: {select_text: true}, value: "curl #{install_board_url(@board)} | ruby"}

      %p You can install commitboards to as many git projects as you like.

  %ul.commits
    = render @board.commits
