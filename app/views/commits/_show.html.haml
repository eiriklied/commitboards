%span.glyphicon.glyphicon-remove.close-card.js-close-card
.card-header
  .h3{ data: { commit: @commit.sha } }= link_to_commit_with_message @commit

= image_tag @commit.image.url, class: 'card-image'

%ul.comments
  = render @commit.comments

= render 'comments/form', comment: @commit.comments.build

