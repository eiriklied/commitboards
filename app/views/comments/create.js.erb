<% if @comment.persisted? %>
  $('.js-card').html('<%= j render('commits/show') %>');
  $('li[data-commit-sha=<%= @commit.sha %>]').find('.comments-count').text('<%= @commit.comments.count %>');
<% end %>
